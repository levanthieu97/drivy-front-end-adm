import * as getters from './getters';
import * as actions from './actions';
import {} from './types';

const state = {};
const mutations = {};

export default {
  namespaced: true,
  state,
  getters,
  actions,
  mutations
};
